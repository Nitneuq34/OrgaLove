bases de données :
  - nom : mysitedb 
    forfait : gratuit
    nom de la base de données : monsite
    utilisateur : monsite

prestations :
  - type : web 
    forfait : gratuit
    nom : monsite
    exécution : python
    Commande de construction : './build.sh' 
    Commande de démarrage : 'uvicorn orga_love.asgi:application --host 0.0.0.0 --port 8000
    envVars :
      - clé : URL_BASE_DE_DONNEES 
        de la base de données :
          nom : mysitedb
          propriété : connectionString
      - clé : SECRET_KEY 
        generateValue : vrai 
      - clé : WEB_CONCURRENCY 
        valeur : 4 